读取：{project} 目录下的所有文件（优先级见下方）

你的任务是为这个小说项目生成（或更新）一份 `CLAUDE.md`，供未来 Claude 会话在续写时快速建立项目上下文。

这份文件是**续写导航**，不是项目资料全集：
- 输出控制在 1500-2500 字，硬上限 3000 字。
- 只保留未来续写必须知道的信息：项目身份、当前进度、故事现状、写作规则和关键禁忌。
- 不复制长篇设定全文、完整总梗概、完整人物档案或日志内容——只引用关键结论。
- 如果内容过多，优先压缩背景复述和已完成的早期细节，保留当前状态、未结清钩子、人物关系现况和风格规则。

## 读取优先级

按以下顺序扫描，靠前的文件优先提取：

1. `.project_info.json` — 项目元数据（题材、规模、进度、幕数、章节数）
2. `03_state/故事总梗概.md` — 故事至今的叙事脉络（如存在）
3. `03_state/状态v*.md`（取最新一份）— 当前场景冻结、关系气压、伏笔状态、节奏状态
4. `04_characters/人物档案.json` + `关系矩阵.json` — 核心角色身份与当前关系
5. `00_baseline/故事主轴.md` + `幕次框架.md` — 故事总设计
6. `00_baseline/核心骨架_*.md` — 各幕结构
7. `00_baseline/世界观.md` — 世界规则
8. `00_baseline/创意方案_*.md` + `文风建议.md` — 风格基调

不存在的文件跳过（post_05b 阶段尚无正文和状态文档，这是正常的）。

## 输出结构

严格按以下章节输出，每节只写关键结论，不展开论述：

```markdown
# {project_name} — 项目 CLAUDE.md

## 一、项目身份

- 题材：
- 规模档：（短篇/中篇/长篇/超长篇，目标字数）
- 语言：
- 参考作品：

## 二、故事概要

用 3-5 句话概括故事的核心设定、主要冲突和情感内核。不超过 200 字。

## 三、当前进度

- 已完成：第 X 幕第 Y 章（正文v{n}.md）
- 当前幕弧位置：（蓄力/升级/转折/高潮/回落）
- 剩余章节数：

（post_05b 阶段填"结构规划完成，尚未开始正文"）

## 四、核心角色现况

每个主要角色 2-3 行：当前处境、核心渴望、核心恐惧、与其他角色的关系现状。不超过 4 个角色。

## 五、关系图谱

用最简洁的方式描述当前关系状态（谁对谁、什么阶段、什么张力）。可以用列表或简表。

## 六、未结清钩子

列出当前悬而未决的钩子（伏笔、关系债务、未兑现承诺、未揭示真相），每条一句话，标注来源章节。

## 七、风格规则

从创意方案和已有正文中提炼的核心风格约束（句式偏好、语域、修辞密度、情绪纹理、叙事视角）。5-8 条，每条一句话。

## 八、关键禁忌

针对本项目的定制化禁忌（不是通用写作常识）。3-5 条，每条说明"禁止什么"和"为什么对这个项目特别重要"。
```

## 更新原则（post_17 阶段）

如果 `{project}/CLAUDE.md` 已存在（说明这不是首次生成），则：
- 保留上一版中仍然成立的风格规则和关键禁忌
- 更新进度、角色现况、关系图谱、未结清钩子至最新状态
- 删除已回收的伏笔和已解决的关系债务
- 不要在更新时膨胀篇幅——如果新增内容导致超出字数，压缩早期已完成的细节

写入绝对路径：`{project}/CLAUDE.md`（项目根目录，与 `.project_info.json` 同级，不在任何子目录里）。
