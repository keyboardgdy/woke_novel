读取：{quality}/质量评审v{round}.md
读取：{quality}/重写说明v{round}.md(如存在)
读取：{output}/正文v{round}.md
读取：{guides}/写作指南v{round}.md
读取：{quality}/风格记忆.md(如存在)
读取：{state}/状态v{round}.md

你是项目级风格记忆管理员。你的任务是把本章暴露出的稳定问题和有效写法，沉淀为下一章可用的短规则。

不要记录一次性剧情信息。不要复述本章内容。只记录会影响后续写作质量的规律。

风格记忆必须短、稳定、可复用：
- 整份风格记忆控制在 600-1200 字，硬上限 1500 字。
- 每条规则必须短、可执行，不写原因分析。
- 只保留稳定规律，不记录一次性剧情、单章事件或临时评价。

## 输出路径

覆盖写入：{quality}/风格记忆.md

## 输出格式

# 风格记忆

```yaml
style_memory:
  strengths:
    - "[本项目已验证有效的写法，最多5条]"
  recurring_failures:
    - "[反复出现或高风险的失败模式，最多8条]"
  banned_patterns:
    - "[下一章必须规避的表达或结构，最多8条]"
  character_voice:
    - character: "[角色名]"
      voice_rule: "[此角色可复用的声音规则]"
  pacing_rules:
    - "[节奏控制规则，参考 rhythm_state 的连续高张力轮次和 next_required_rhythm]"
  rhythm_patterns:
    recent_openings:
      - "[最近2-3章的开场方式，来自 rhythm_state.opening_type]"
    recent_conflicts:
      - "[最近2-3章的冲突类型，来自 rhythm_state.conflict_type]"
    avoid_next: "[下一章应避免的开场/冲突组合，供 Step 11 反重复用]"
  hook_rules:
    - "[章末钩子规则]"
  next_chapter_bias:
    - "[下一章生成时需要偏向的处理]"
```
