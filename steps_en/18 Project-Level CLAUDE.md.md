Scan the current project: `{project}`

Based on the files above, rewrite a complete `CLAUDE.md` that allows future Claude sessions to quickly understand the project context.

Core requirement:

`CLAUDE.md` should be clean. It should answer only one question:

> "Who am I, what am I writing, and how should I write it?"

Write to absolute path: `{project}/CLAUDE.md` (the project root, at the same level as `.project_info.json`, not inside any subdirectory).

Output path: `{project}/CLAUDE.md`
